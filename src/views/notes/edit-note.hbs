<div class="animate__animated animate__fadeIn row">
<div class="col-md-10 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3 style="text-align:center;">Editar estación de monitoreo "{{note.title}}" </h3>
        </div>
        <div class="card-body">
             <div class="container-fluid" id="map-template"></div>
             <script src="/js/map_edit_main.js"></script>
             <script>

                             L.marker( [{{note.description}}], {icon: parkIconoff} ).addTo(map)
                             .bindPopup('{{note.title}}');  
                                   
                        </script>
                        <hr class="my-4">
            <form action="/notes/edit-note/{{note._id}}?_method=PUT" method="POST">
                <input type="hidden" name="_method" value="PUT">
                <div class="form-group">
                    <input type="text" name="title" class="form-control" placeholder="Title" value="{{note.title}}">
                </div>
                 <div class="form-group">
                    <textarea name="topic" class="form-control">{{note.topic}}</textarea>
                </div>
                <div class="form-group">
                    <textarea id ="new_coor" name="description" class="form-control">{{note.description}}</textarea>
                </div>
                <div class="form-group">
                    <button class="btn btn-primary btn-block" type="submit">
                        Guardar
                    </button>
                    <hr class="my-1">
                    <a href="/notes" class="btn btn-secondary btn-block" role="button" aria-pressed="true">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>
</div>