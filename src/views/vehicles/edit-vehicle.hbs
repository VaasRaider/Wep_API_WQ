<div class="animate__animated animate__fadeIn row">
<div class="col-md-10 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3 style="text-align:center;">Editar vehículo "{{vehicle.placas}}" </h3>
        </div>
        <div class="card-body">
            <form action="/vehicles/edit-vehicle/{{vehicle._id}}?_method=PUT" method="POST">
                <input type="hidden" name="_method" value="PUT">
                <div class="form-group">
                    <input type="text" name="placas" class="form-control" placeholder="Title" value="{{vehicle.placas}}">
                </div>
                <div class="form-group">
                    <textarea id ="new_coor" name="description" class="form-control" placeholder="Coordenadas">{{vehicle.description}} </textarea>
                </div>
                <div class="form-group">
                    <button class="btn btn-primary btn-block" type="submit">
                        Guardar
                    </button>
                    <hr class="my-1">
                    <a href="/vehicles" class="btn btn-secondary btn-block" role="button" aria-pressed="true">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>
</div>